# 使用说明 · 日常用法与故障排查

> 面向使用者（非技术人员）。全程只需在网页上点，不需要安装任何软件。

---

## 一、这个系统是什么

三份学习页面，配套使用：

| 入口 | 干什么 | 体量 |
|---|---|---|
| **学习地图** | 学什么 · 按岗位分类 | 38 个技能卡 + 沙盘 + 剧本 + 实验包 |
| **面试八股** | 开口讲 · 面试问答 | 177 题 / 15 模块 |
| **学习题库** | 选得对 · 刷题验证 | 536 题 / 13 模块 |

打开网址：**https://yxiaof.github.io/ops-study/**

电脑和手机打开的是**同一个网址**。开启同步后，两边共用同一份进度。

---

## 二、首次配置（只做一次，约 12 分钟）

### 第 1 步：拿一把「钥匙」

这把钥匙叫**令牌**，作用是让网页有权把进度写进您的网盘柜子。

1. 打开 GitHub → 右上角**头像** → **Settings**
2. 左侧菜单拉到最下面 → **Developer settings**
3. 左侧 → **Personal access tokens** → **Fine-grained tokens**
4. 点 **Generate new token**
5. **Expiration**（有效期）选 **90 days**
6. 页面上找到 **Permissions** 这一块，确认它显示的是 **Account** 标签
   （点了才是对的，Gists 属于 Account 权限）。点右边的 **「+ Add permissions」** 按钮，
   会弹出「Select account permissions」小窗。
   **这是最关键也最容易卡住的一步** —— Gists 不在默认列表里，如果不点这个按钮，
   页面会一直显示 *No account permissions*。
7. 小窗里有一个搜索框。**在搜索框里输入 `gists`。**
   ⚠️ **不要输 `read`** —— 这个框是按**权限名称**搜索的，不是按权限值搜索的，
   输 `read` 会显示 *No items available*（看起来像"根本没这个权限"，其实是搜错了词）。
   也可以完全不搜，直接把清单往下滚，在 **Account permissions** 分组里找 **Gists**。
8. 找到 **Gists** 后，把右边的下拉从 `No access` 改成 **`Read and write`**，
   然后按 **Enter** 或点小窗外的空白处收起。
   **其他所有项一律保持 No access**（等于把钥匙的权限锁到最小）。
9. 点最下方绿色按钮 **Generate token** → **立刻复制**那串 `github_pat_` 开头的字符

> **万一在小窗里怎么都找不到 Gists**（个别账号的页面确实不显示它），
> 不用继续折腾，直接改用下面这把「经典钥匙」，效果完全一样、步骤更少：
>
> 1. Settings → Developer settings → **Personal access tokens** → **Tokens (classic)**
> 2. 右上角 **Generate new token** → 选 **Generate new token (classic)**
> 3. **Note** 随便写（例如 `ops-study`）；**Expiration** 选 **90 days**
> 4. 下面是一长列复选框，**只勾选一个：`gist`**（位置在 `repo` 下面第二个）。
>    其余全部保持不勾 —— 不勾就等于没给权限。
> 5. 拉到最底 **Generate token** → 复制 `ghp_` 开头的那串字符
> 6. 之后的第 2~4 步完全照旧，粘贴的位置一模一样。
>
> 两种钥匙**二选一**即可，不用都拿。

> 顺带说明：上面「Repository access」那一段（Public repositories / All repositories /
> Only select repositories）**对 Gists 没有影响**，随便选哪个都行，不用纠结。

> 这串字符**只显示一次**，页面关掉就再也看不到了。复制好先粘到记事本暂存。
> 如果真丢了，回这个页面重新生成一把即可，旧的一键作废。

### 第 2 步：粘贴钥匙（电脑）

1. 打开网址 → 进入**任意一份**页面（建议先从「学习题库」开始）
2. 点右上角的 **「☁️ 同步」**
3. 把钥匙粘进**第 1 栏**（令牌）
4. **第 2 栏「进度柜子地址」请保持完全空白** —— 这一栏是给「已经在别的设备上建过柜子」的老用户填的；
   新用户填了反而会报错（找不到那个柜子）
5. 点 **「保存并启用」** → 会弹一个确认框问「是否用这把令牌自动创建一个？」→ 点 **确定**
6. 约 3 秒后弹出「进度柜子已创建并启用」，就成功了

进度柜子会**自动帮您建好**，不用去 GitHub 手动建。

看到状态变成 **「已开启」** 就成功了。

### 第 3 步：另外两份也粘一次

回到首页 → 进「面试八股」→ 同样点「☁️ 同步」→ **粘同一把钥匙** → 保存并启用。
「学习地图」再重复一次。

> 三份共用一个柜子、各占一格，互不覆盖。

### 第 4 步：手机也粘一次

iPhone 用 **Safari** 打开同一个网址 → 找到「☁️ 同步」→ 粘同一把钥匙 → 保存并启用。

**建议顺手加桌面图标**：Safari 打开网址 → 底部中间的「**分享**」按钮 ⬆️ → 往下找「**添加到主屏幕**」→ 添加。
之后桌面会多一个图标，点开是全屏的，像 App 一样。

---

## 三、日常使用：什么都不用点

配好之后，正常做题就行。系统会自动：

| 时机 | 动作 |
|---|---|
| 停手 **30 秒** | 自动上传一次（主路径） |
| 连续做题满 **5 分钟** | 强制上传一次（防止老是等不到 30 秒空档） |
| **切走 / 锁屏 / 关页面** | 立刻上传（不等倒计时） |
| 打开页面 | 自动下载云端最新进度，接着上次继续 |

- 网络不好时上传失败**不会丢数据**，下次打开自动补传。
- 如果云端有更新的进度（比如手机上刚做了一批），会**弹窗问您**要不要用，不会闷头覆盖。

---

## 四、状态灯怎么看

右上角那个按钮上的小圆点：

| 显示 | 含义 |
|---|---|
| 灰色 · 未配置 | 还没填钥匙，此时纯本地记录，一切功能正常 |
| 绿色 · 已同步 12:34 | 正常，最近一次上传在 12:34 |
| 黄色 · 正在上传 | 正在传，稍等 |
| 红色 · 同步失败 | 见下方排查表 |

点开面板能看到详细状态：本机记录多少条、上次上传/下载的时间。

---

## 五、故障排查

| 现象 | 原因与处理 |
|---|---|
| 面板提示**「钥匙无效」** | 令牌复制时少了字符，或有效期已过。重新生成一把再粘一次 |
| 面板提示**「没找到柜子」** | 首次配置时柜子没建成功。把钥匙重新粘一次，让它再建一遍 |
| 状态一直**红点 / 同步失败** | 先确认手机或电脑能正常打开 github.com；能打开就再点一次「立即上传」 |
| **两台设备记录不一致** | 在记录少的那台点「立即下载」，或直接刷新页面（打开时会自动拉取） |
| 换了手机 / 换了电脑 | 打开网址 → 粘**同一把钥匙** → 自动接上全部历史记录 |
| 提示要**重新登录 GitHub** | 令牌 90 天到期了，回到第 1 步重新生成一把 |
| 点「保存并启用」**完全没反应**（按钮像坏的） | 页面是浏览器缓存的旧版本。**关掉这个标签页重新打开网址**；iPhone 上可在网址后加 `?v=2` 强制刷新，或等 10 分钟再试 |
| 提示**「启用失败」**含 404 | 第 2 栏填了东西。把它**完全清空**，再点一次「保存并启用」 |
| 提示**「启用失败」**含 401 / 403 | 令牌没勾 Gists 权限、或已过期。回第 1 步重新生成一把 |
| 权限弹窗里**搜 `read` 显示 No items available** | 那个搜索框按权限**名称**匹配，不按权限值匹配。清空后改成输入 **`gists`**；也可以不搜，直接把清单往下滚 |
| 生成令牌时**找不到 Gists** | ①确认下面显示的是 **Account** 标签；②先点 **+ Add permissions** 调出清单；③仍找不到就别耗着，按第 1 步末尾的「经典钥匙」备选走，勾 `gist` 一个框就够 |
| 手机上**找不到同步按钮** | 横屏或放大页面看看；按钮在页面右上角 |

---

## 六、几条要记住的规矩

1. **认准一个浏览器用。** 不同浏览器的本地记录各存各的（Safari 和 iOS 版 Chrome 也不互通）。虽然开了云同步之后能拉回来，但本地缓存切换会多一次等待。
2. **同一时间只在一台设备上做题。** 两边同时做，进度会以"谁答得多谁为准"合并，另一边的部分记录可能被覆盖。
3. **钥匙不要发到群里，也不要写进代码。** 它只存在您自己的设备里。
4. **不要清理浏览器数据。** iPhone 上：设置 → Safari → 清除历史记录与网站数据，会把本地缓存一并清掉。开了云同步的话，清了也不怕——重新打开就自动拉回来。
5. **不用天天备份。** 云端就是记录的主份。

---

## 七、数据放在哪（隐私边界）

| 内容 | 位置 | 是否公开 |
|---|---|---|
| 三份学习页面（题目） | 公开仓库 `ops-study` | 公开（无所谓，题目本身不是隐私） |
| **您的做题进度** | GitHub 的 **secret Gist**（网页自动建） | **不公开**，只有知道那串随机地址的您能访问 |
| 令牌（钥匙） | 只存在您自己的设备浏览器里 | 不上传、不公开、不经过任何第三方 |

打开 Gist 的方式：登录 GitHub → 头像 → **Your gists**，能看到那个自动创建的文件柜。
