# ops-study 代码规范（小白版）

> 这份文档是《代码规范》的**人话版**。每一条都告诉你：规定是什么 + 为什么 + 对你来说具体怎么做。  
> 读完后你会明白：这个项目像个什么物件，哪些动作会弄坏它，以及你每天要做对哪几件事。

---

## 开篇：先用 1 分钟搞懂这个项目是什么

**这个项目 = 几个「自带全部零件」的网页文件。**

想象一个便携工具箱：锤子、螺丝刀、卷尺全部焊死在一个盒子里，走到哪提到哪，打开就能用，不需要任何安装。

我们的学习系统就是这样：

- 每个页面（学习题库.html、八股文.html……）都是**一个文件装下所有东西**——界面样式、程序逻辑、题库数据，全在里面。
- 不需要安装软件、不需要联网就能打开（存到手机里都行）、不需要任何"编译"步骤。
- 部署方式也简单到极致：把文件推到 GitHub，Pages 自动展示，完事。

**整份规范只为了守住这一件事：别把这个"一体化工具箱"拆散。**

后面所有"禁止"，90% 都是防止有人好心办坏事，把它拆成需要安装、需要编译、需要联网的一堆零件。

---

## 一、项目形态约定（最重要的三条铁律）

### 铁律 1：只用最原始的网页技术，不加任何"高级装备"

**规定**：只用 HTML + CSS + JavaScript 三样原生的东西。禁止 React/Vue（框架）、webpack/vite（打包工具）、npm 依赖（第三方软件包）、CDN 外链（从别人服务器加载的代码）。

**人话**：

- **框架（React/Vue）**&#x662F;什么：大公司做复杂应用用的"脚手架"。为什么禁？——它需要一套安装+编译流程才能运行，我们的页面就会从"双击就能打开"变成"必须先装环境才能跑"。而且我们的项目用不上它的好处，纯属负重。
- **打包工具**是什么：把源代码"加工"成能运行的版本的机器。为什么禁？——加工后有源码和成品两套东西，你改了代码但忘跑加工步骤，线上还是旧的，协作时天天出鬼。
- **CDN 外链**是什么：页面里写一行，从别人的服务器加载代码。为什么禁？——①没网就废；②对方服务器被黑/被墙，你的页面就挂了或被塞进恶意代码。我们的页面里可是有用户粘 GitHub 令牌的框，安全红线。

**你该怎么做**：想实现任何功能，先想"原生三件套怎么写"；写不出来就问群里，不要随手 `npm install` 或复制带 `<script src="https://...">` 的代码。

### 铁律 2：一个页面 = 一个文件，样式和数据都写在里面

**规定**：CSS 写在文件开头的 `<style>` 标签里，JS 写在文件末尾的 `<script>` 标签里，数据也内嵌。**不要**拆成 style.css、app.js 分文件。

**人话**：这就像把说明书、工具、配件都钉在同一个盒子里。分开的好处（代码整洁）我们不需要，分开的坏处（拷贝文件 = 拷不完整、进度导出功能直接坏掉）我们扛不起。

### 铁律 3：不要动 `.nojekyll` 这个文件

**规定**：仓库根目录的 `.nojekyll`（一个空的隐藏文件）不许删。

**人话**：它只干一件事——告诉 GitHub Pages"别对我的文件做额外加工"。删了它，中文名文件可能就打不开了。它存在感为零但删了要命，所以单独列一条。

**另外记住**：所有文件都是 UTF-8 编码（存中文的国际通用标准）。用 Windows 记事本等编辑器保存时注意选 UTF-8，否则中文全变乱码。

---

## 二、HTML 规范（页面的"骨架"）

### 1. 文件开头三行"身份证"必须齐全

```html
<html lang="zh-CN">                              ← 告诉浏览器：我是中文页面
<meta charset="UTF-8">                           ← 告诉浏览器：用 UTF-8 读我（防中文乱码）
<meta name="viewport" content="width=device-width, initial-scale=1.0">
                                                 ← 告诉手机：按手机屏幕宽度显示（防字小成蚂蚁）
```

**人话**：三行都不可见、缺了也不报错，但缺了就是——乱码、手机上没法看。新建页面从现有文件复制这三行，永远别手敲漏掉。

### 2. 用"有名字"的标签，别全用 `<div>`

**人话**：`<div>` 是匿名盒子，`<header>` 是页头、`<footer>` 是页脚、`<section>` 是一个板块。用有名字的标签，别人看代码立刻知道哪块是啥；无名的盲人读屏软件也读不懂。现有代码已经这么写了，照着抄结构就行。

### 3. 可点击的东西分两种，别混用

- **跳转到别的页面** → 用 `<a href="...">`
- **在当前页面做动作**（弹窗、保存、复制）→ 用 `<button type="button">`

**人话**：button 里如果不写 `type="button"`，在某些情况下点击会触发"提交表单"导致页面刷新，坑人的隐性炸弹。

### 4. 给元素起 id 名字有固定套路

- 按钮叫 `btn` 开头：`btnSync`（同步按钮）、`btnExport`（导出按钮）
- 手机专属组件叫 `m` 开头：`mMenu`
- 视图容器叫 `xxxView`：`practiceView`
- 其他直接用英文驼峰描述：`syncStatus`（同步状态）

**人话**：驼峰 = 多个单词连写、从第二个词起首字母大写，像 `btnSync`。套路统一后，看到名字就知道它是干嘛的。

### 5. 新功能必须在手机上能看

项目一半用户是手机（Safari 打开），每份页面里都有一段专门管手机布局的代码（`@media` 开头那块）。你加了新东西，**自己拿手机或把浏览器窗口拖窄到手机宽度试一遍**，别只看电脑效果。

---

## 三、CSS 规范（页面的"皮肤"）

### 1. 颜色不许自己现编，必须用"公用的颜色罐子"

文件开头有个 `:root` 区块，定义了全家桶颜色：`--blue`（主蓝）、`--red`（红）、`--green`（绿）……

**规定**：写颜色一律写 `color: var(--blue)`，**不许**直接写 `color: #2563eb`。

**人话**：为什么？假设哪天要把主色从蓝改成紫，公用罐子只需改一行；到处硬编码的话，得全文件翻几十处，漏一处就花色。新罐子颜色也要先加到 `:root` 再用。

### 2. 类名起名：全小写，单词用 `-` 连接

比如 `qcard`（题目卡片）、`goal-bar`（进度条）。不写大写、不用下划线。跟着现有代码的风格走。

### 3. 样式分区要写注释

现有代码里有 `/* ---- 云同步 ---- */`、`/* 手机版模式 */` 这样的分区标记。你加新样式时也照这个格式加一行，让下个人能找到。

### 4. 按钮点上去要有反馈，键盘操作也要能看见焦点

链接和按钮要有 hover 变化（鼠标悬停）、`:focus-visible` 样式（用 Tab 键走到它时有个描边）。现有代码有，别省——这不是美学，是可访问性（盲人/键盘用户靠它）。

### 5. 不许用 `!important`

它是 CSS 里的"霸道条款"，用一次就把别人的规则压死，后面维护的人会恨你。要覆盖某个样式，把选择器写得更具体一点就行。

---

## 四、JavaScript 规范（页面的"大脑"）

### 1. 开头写 `"use strict";`

**人话**：严格模式开关。开了之后，一些随手写错（比如变量名打错字）会直接报错而不是悄悄装没事——早报错早发现。

### 2. 声明变量只用 `const` 和 `let`，禁用 `var`

**人话**：三者都是"起个变量名"，但 `var` 是上古写法，作用域规则诡异容易出 bug。规则：默认用 `const`（= 这个名字不会重新赋值），需要变再用 `let`。

### 3. 网络请求用 fetch + async/await，且必须"防摔"

所有访问网络的代码（比如同步到 Gist）都要包在 try/catch 里，失败了要在界面上给用户提示。

**人话**：try/catch = 安全网。网络这种东西随时会断，不兜底的话用户看到的就是"点了没反应"，比报错更吓人。规矩是：**宁可红字报错，不许无声失败**。

### 4. 函数起名：动词开头

`renderXxx`（渲染）、`handleXxx`（处理点击）、`loadXxx`（加载）、`saveXxx`（保存）。一看名字知道动作。

### 5. 注释用中文，按功能分大块

照现有格式：

```js
/* ================== 题库数据 ================== */
/* ================== 同步逻辑 ================== */
```

**重要**：好注释写"为什么"，不是"做什么"。比如同步逻辑里写一行「上传前先拉云端合并，防止两台设备互相覆盖」——这解释的是设计意图，比十行废话注释值钱。

### 6. 定时器要收拾干净

自动上传用的倒计时，在用户切走页面/锁屏时要正确处理（现有代码用页面可见性事件处理了）。改同步逻辑时注意别把这套节奏改坏。

---

## 五、数据规范（题库和进度）

### 1. 题目格式：一题一行，字段名不许改

每道题是压缩成一行的小对象，字段是全仓库统一约定的：

| 字段     | 意思                            | 你的操作                    |
| ------ | ----------------------------- | ----------------------- |
| `id`   | 题目唯一编号，格式 `M模块-序号`（如 `M1-01`） | 新题接着最后一个编号往下排，不许重复      |
| `m`    | 模块编号（数字）                      | 按所属模块填                  |
| `lv`   | 难度 1–5                        | 按实际难度                   |
| `t`    | 题型                            | 沿用现有取值，别自创新类型           |
| `stem` | 题干                            | 必填                      |
| `opts` | 选项                            | 选择题必填                   |
| `ans`  | 答案                            | 必填                      |
| `ex`   | 中文解析                          | **必填，不许空着**——没解析的题等于没教人 |

### 2. localStorage 键名有命名规律，新键必须跟

**先解释 localStorage**：浏览器给每个网站的一块"本地小仓库"，页面存进度用的。关掉浏览器数据还在，清浏览器数据就没了。

- 存学习数据的键：`ops` 开头 + 域名 + 版本号，如 `opsquiz_v1`（题库 v1）、`opsmap.v1`（地图 v1）
- 存同步配置的键：在数据键后面加 `_sync`，如 `opsquiz_sync`

**关键规矩**：如果数据的内部结构要改（比如给题目加字段），**不能**直接改旧键里存的东西的格式——必须把版本号升一级（`v1` → `v2`）并写一段"旧数据搬到新格式"的代码。否则所有老用户打开页面直接读不出来，进度全"消失"。

### 3. 数据红线

学习进度以外的任何东西——GitHub 令牌、姓名、手机号——**绝对不写进代码、数据、Gist**。详见下一节。

---

## 六、云同步与隐私（安全红线，最高优先级）

**先解释这套同步的原理**：用户的进度通过 GitHub 的 Gist（一种私人网盘小柜子）在多台设备间同步；访问柜子用的钥匙是"令牌"（`github_pat_` 或 `ghp_` 开头的一串字符）。

三条红线：

1. **令牌只存在用户自己手机的浏览器里，永远不出现在任何提交内容中**。不写进代码、不写进注释、不截图发群里、不写进 commit 说明。谁把令牌提交进了仓库，那把令牌立刻作废重来，并且要在群里说明。
2. **同步必须是"合并"而不是"覆盖"**。现在的逻辑是：两台设备各做各的，进度取并集合并，谁都不丢。改这块逻辑是全仓库最高危操作，必须先跟负责人对齐再动手，commit 里要显著写明。
3. **同步失败不许丢本地数据**。网络断了就先存本地，下次再传。

---

## 七、Git 与协作流程（怎么交作业）

**先解释 Git 三板斧**：`git add`（把改动放进待提交清单）→ `git commit`（打包成一个存档点）→ `git push`（上传到 GitHub）。每次 commit 是一个"存档点"，出问题可以回退到任意存档点。

### 1. 别直接改主分支，开个"工作分支"

- 分支 = 平行工作副本，随便折腾不影响主线。
- 命名有格式：加功能 `feat/xxx`、修 bug `fix/xxx`、改文档 `docs/xxx`、动数据 `data/xxx`。
- 完成后经负责人确认再合并回去。

### 2. commit 信息要让人看懂你干了啥

好例子（仓库里真实存在）：

```
fix: 多设备进度改为按项合并（取并集），不再整份互相覆盖

- opsquiz_sync 上传前先拉取云端并按 id 合并
- opsbgw / opsmap 同步处理
```

坏例子：`update`、`改了一些东西`——这种存档点等于没写，回退时两眼一抹黑。

### 3. 提交前过一遍自查清单（照着念一遍，30 秒）

- [x] 电脑上双击文件直接打开（不联网）也能正常用
- [x] 网页版打开也正常
- [x] 浏览器窗口拖窄到手机宽度，布局不乱
- [x] 开同步、关同步两种状态下都不报错
- [x] 全文搜一遍，没有令牌/密钥/个人信息
- [x] 文件还是 UTF-8 编码（打开不乱码）

### 4. 一次 commit 只干一件事

改 bug 是改 bug，加功能是加功能，分开提交。混在一起的话，想撤销其中一件就等于全撤。

### 5. 两个高压线

- 未经确认不 force push（暴力覆盖远端历史，会吃掉别人的提交）
- 不乱删文件；删大文件前先在群里确认没人用

---

## 八、文档规范

- 文档全用中文 Markdown，文件名中文名（跟《使用说明.md》风格一致）。
- 开头一句话讲清"给谁看的、解决什么问题"。
- 教程类文档要**步骤化**：写按钮上实际显示的文字（「☁️ 同步」「保存并启用」），不写"点击同步功能"这种抽象话——参考《使用说明.md》的水准。
- **改了代码行为，必须同步改文档**。文档说"停手 30 秒自动上传"，代码改成了 60 秒却没改文档，用户会以为系统坏了。

---

## 九、新增一个页面的验收标准

想加一份新的学习页面（比如"面试模拟.html"）？必须做到这五件事才算合格：

1. 右上角有「☁️ 同步」按钮和状态灯（灰=未配置、绿=已同步、黄=上传中、红=失败），和其他页面长一样。
2. 进度存 localStorage，停手 30 秒自动上传，关页面/切走时立刻上传。
3. 手机宽度下能正常用。
4. 在 `index.html` 首页的卡片导航里登记入口，并加上进度读取。
5. `<title>` 按"运维学习… · 规模描述"格式写。

少一件，用户就会问"为什么这个页面手机上没法同步？"

---

## 十、违规了会怎样

| 行为                  | 后果                      |
| ------------------- | ----------------------- |
| 把令牌/密钥提交进仓库         | 令牌立即作废、重写历史清除记录、提交者群里说明 |
| 破坏"单文件自包含"（拆文件/加依赖） | 合并请求直接 revert（撤销）       |
| 破坏"并集同步"语义（覆盖式同步）   | 同上                      |

**这两条铁律 = 这个项目的命根子**：一个管"拷走就能用"，一个管"多设备进度不丢"。

---

## 附：小白速查卡（截图存手机）

**每天写代码时记住 6 句话：**

1. 不装任何新东西，原生三件套解决一切
2. 一个页面一个文件，样式数据都内嵌
3. 中文注释写"为什么"，变量命名照旧例
4. 颜色用 `var(--xxx)`，字段名一个都不许改
5. 令牌不进仓库，同步只合并不覆盖
6. 交作业：开分支 → 写清楚 commit → 过自查清单 → 等确认

**拿不准的时候**：翻现有代码找同样场景怎么写的，照着抄结构——现有代码就是最好的范例。

---

*本规范解释权归仓库负责人；觉得哪条不合理，先提出来讨论，别先斩后奏。*
