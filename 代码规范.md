# ops-study 代码规范

> 版本：v1.0（草案，待确认）
> 适用范围：本仓库全部代码、数据、文档与 Git 操作。
> 原则：本仓库是**零构建、零依赖的纯静态单文件应用**，规范的目标是保持「拷走一个文件就能用」的特性，同时让多人协作时不互相踩坏。

---

## 一、项目形态约定（先读懂再动手）

| 项 | 约定 | 说明 |
|---|---|---|
| 技术栈 | 原生 HTML + CSS + JavaScript（ES6+） | **禁止引入**框架（React/Vue）、构建工具（webpack/vite）、npm 依赖、CDN 外链脚本 |
| 文件组织 | 每个页面 = 一个自包含 HTML 文件 | CSS 在 `<head><style>`，JS 在 `</body>` 前的 `<script>`，数据和逻辑同文件 |
| 部署 | GitHub Pages，`docs/update-documentation` 分支 | 根目录保留 `.nojekyll`，禁止删除 |
| 运行环境 | 现代浏览器（Chrome / Safari / iOS Safari / 手机浏览器） | 不支持 IE；新 API 使用前先确认 Safari 兼容性 |
| 字符编码 | 全部 UTF-8，`<meta charset="UTF-8">` 必须保留 | 文件名与内容含中文是本仓库常态，**编辑器请确认 UTF-8 无 BOM** |

**单文件铁律**：不要为了"工程化"把 CSS/JS/数据拆成独立文件——除非获得仓库负责人同意并更新本规范。

---

## 二、HTML 规范

1. `lang="zh-CN"`、`charset`、`viewport` 三件套必须齐全。
2. 语义化标签优先：`header / section / footer / nav`，不滥用 `<div>`。
3. 所有可交互元素用 `<button type="button">`（避免误提交），跳转用 `<a href>`。
4. DOM 元素 `id` 命名遵循 camelCase + 职责前缀：
   | 前缀 | 用途 | 示例 |
   |---|---|---|
   | `btn` | 按钮 | `btnSync`、`btnExport` |
   | `m` | 移动端专属组件 | `mMenu`、`mTabs` |
   | `xxxView` | 页面视图容器 | `practiceView`、`examView` |
   | `syncStatus` 等 | 状态展示元素 | camelCase 直述 |
5. 移动端适配必须保留：`@media (max-width:640px)` 断点 + `body.mobile` 类切换，新功能上线前**必须**在手机宽度下自查一遍。

## 三、CSS 规范

1. **颜色一律走 `:root` CSS 变量**，禁止在新增代码里硬编码颜色值（现有 `--blue/--ink/--sub/--line/--bg/--card/--green/--amber/--red` 优先复用；确需新色先在 `:root` 定义）。
2. 类名：小写、简短，单词间连字符（`qcard`、`goal-bar`、`score-box`）；不要用下划线或大写。
3. 缩进 2 空格；相关属性可紧凑单行书写（跟随现有风格），但**同一选择器不要一半单行一半多行混排**。
4. 按功能分区注释，沿用现有格式：
   ```css
   /* ---- 云同步 ---- */
   /* 手机版模式 */
   /* P0：今日练习 / 热图 / 诊断面板 */
   ```
5. 交互反馈三件套不省略：`:hover`、`:focus-visible`（键盘可达性）、transition 时长 ≤ 0.2s。
6. 禁止 `!important`（现有代码没有，别开坏头）；确需覆盖时提高选择器优先级并写注释说明原因。

## 四、JavaScript 规范

1. 每个独立 `<script>` 块以 `"use strict";` 开头。
2. **一律 `const` / `let`，禁止 `var`**；优先 `const`。
3. 异步操作统一 `async/await` + `fetch`；所有网络请求**必须**有 try/catch 兜底，失败时 UI 给出可读提示（不许静默失败）。
4. 需要隔离作用域的逻辑用 IIFE 包裹（跟随 `index.html` 现有写法）。
5. 函数命名 camelCase，动词开头：`renderXxx`、`handleXxx`、`loadXxx`、`saveXxx`。
6. 注释使用**中文**，按功能分区：
   ```js
   /* ================== 题库数据 ================== */
   /* ================== 同步逻辑 ================== */
   ```
   复杂逻辑（如合并策略、倒计时）必须写"为什么"，不只是"做什么"。
7. 禁止引入第三方库；确需 polyfill 级能力，先手写兜底分支（参考 `index.html` 中 clipboard 的 fallback 写法）。
8. 定时器（如 30 秒自动上传）在页面 `visibilitychange` / `beforeunload` 时必须正确处理，不允许后台堆积。

## 五、数据规范（题库 / 进度结构）

1. 题库使用内嵌紧凑 JSON 数组，**一题一行**，字段名沿用现有约定，**不得擅自改名**：
   | 字段 | 含义 |
   |---|---|
   | `id` | 全局唯一，格式 `M<模块>-<序号>`（如 `M1-01`） |
   | `m` | 模块编号（整数） |
   | `lv` | 难度 1–5 |
   | `t` | 题型：`s` 单选 / 多选 / 开放题（沿用现有取值） |
   | `tags` | 标签数组 |
   | `stem` | 题干 |
   | `opts` | 选项 |
   | `ans` | 答案 |
   | `ex` | 中文解析 |
2. 新增题目必须带解析（`ex` 不允许留空）。
3. localStorage 键名沿用现有命名体系，新增键必须遵守：
   - 数据键：`ops<域>.v<版本>`（如 `opsquiz_v1`、`opsmap.v1`、`opsbgw.v1`）
   - 同步配置键：对应数据键 + `_sync` 后缀（如 `opsquiz_sync`）
   - **结构变更必须升版本号并写迁移逻辑**，禁止原地改旧版本键的语义。
4. 上传到 Gist 的内容只允许学习进度类数据；**任何令牌、密钥、个人身份信息禁止写入代码、数据或 Gist**。

## 六、云同步与隐私红线

1. 令牌（`github_pat_` / `ghp_` 开头）**只存在于用户本地 localStorage**，任何情况下不得出现在代码、注释、文档截图、commit 信息里。
2. Gist 一律使用 **secret Gist**；同步策略改动时必须保持「多设备按项合并（取并集）、不整份覆盖」的现有语义——修改合并逻辑属于高危变更，需在 commit 信息中显著标注并经负责人 review。
3. 同步失败不得丢本地数据：先落本地，再补传。

## 七、Git 与协作流程

1. **分支模型**：
   - `main`：稳定发布分支，直接服务于 GitHub Pages 可见的正式内容（如启用 Pages 于 main）。
   - 功能分支命名：`docs/<主题>`、`feat/<主题>`、`fix/<主题>`、`data/<主题>`；完成后经确认合并。
2. **Commit 信息**：中文，一行主题 + 空行 + 正文要点，格式沿用现有风格：
   ```
   fix: 多设备进度改为按项合并（取并集），不再整份互相覆盖

   - opsquiz_sync 上传前先拉取云端并按 id 合并
   - opsbgw / opsmap 同步处理
   ```
   禁止 "update"、"修改一些东西" 这类无信息量提交。
3. **提交前自查清单**：
   - [ ] 页面在浏览器直接打开（file:// 与 Pages 两种方式）功能正常
   - [ ] 手机宽度（≤640px）下布局不破
   - [ ] 同步开关状态下都不报错
   - [ ] 全文无令牌/密钥/个人隐私
   - [ ] 文件编码 UTF-8 无 BOM，无 CRLF 混入（`.gitattributes` 可后续固化）
4. 单次 commit 控制在一个主题；大改动拆分提交，便于回滚。
5. 合并进 `main` / `docs` 主分支前需仓库负责人确认；**未经确认不得 force push 公共分支**。

## 八、文档规范

1. 文档使用中文 Markdown，文件名与现有风格一致（中文名 + `.md`）。
2. 每份文档开头一句话说明"面向谁、解决什么"。
3. 面向使用者的操作文档（如《使用说明.md》）要求：步骤化、表格化排障、给出可点的按钮名而非抽象描述。
4. 代码行为变更时，同步更新《使用说明.md》与 `index.html` 上的说明文字，不允许文档与行为脱节。

## 九、新增页面的最小要求

新增一份 HTML 学习页面时，必须具备：

1. 与现有三页一致的「☁️ 同步」入口与状态灯（灰/绿/黄/红）。
2. 本地进度（localStorage）+ 停手 30 秒自动上传 + 页面关闭前强制上传。
3. 移动端响应式适配。
4. 在 `index.html` 导航卡片区登记入口与进度读取。
5. 头部 `<title>` 格式：`运维学习… · <规模描述>`。

## 十、违规处理

- 含令牌/密钥的提交：一经发现立即作废该令牌并重写历史，责任提交者需在群内说明。
- 破坏「单文件自包含」「并集同步」两条铁律的合并：直接 revert。

---

**本规范自确认之日起生效；修改需经仓库负责人同意并升版本号。**
